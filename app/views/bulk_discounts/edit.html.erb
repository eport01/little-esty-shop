<h4><center> Edit Discount Page</center><h4>

<%= render partial: 'form', locals: {
                                      path: "/merchants/#{@merchant.id}/bulk_discounts/#{@bulk_discount.id}",
                                      method: :patch,
                                      button_text: "Submit",
                                      discount: @bulk_discount.discount,
                                      quantity_threshold: @bulk_discount.quantity_threshold,
                                      } %>


